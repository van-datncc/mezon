import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    container: {
        width: "100%",
        flexGrow: 1,
        flexBasis: 500,
        overflow: "hidden"
    },
})

export default styles;