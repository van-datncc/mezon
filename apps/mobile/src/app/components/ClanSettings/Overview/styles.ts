import { baseColor, Colors } from "@mezon/mobile-ui";
import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    headerActionTitle: {
        color: baseColor.blurple,
        fontWeight: "bold",
        paddingHorizontal: 20,
    },
    container: {
        paddingHorizontal: 20,
        paddingVertical: 10
    },
})

export default styles;