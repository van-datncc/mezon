import { baseColor, Block } from '@mezon/mobile-ui';
import React from 'react';

export default function StreamingScreen() {
	return <Block width={'100%'} height={400} backgroundColor={baseColor.black}></Block>;
}
