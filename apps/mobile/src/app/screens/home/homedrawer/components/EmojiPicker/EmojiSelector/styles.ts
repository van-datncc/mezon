import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    cateContainer: {
        display: "flex",
        flexDirection: "row",
        gap: 10,
    },

    cateItem: {
        padding: 5
    }
})

export default styles;